using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace DocsWASM.Shared.Helpers
{
	public class SvgHelpers
	{
		/*private HtmlAgilityPack.HtmlDocument document = new HtmlAgilityPack.HtmlDocument();
		public string SvgString { get;}

		public SvgHelpers(string svgString)
		{
			this.SvgString = svgString;
			document.LoadHtml(SvgString);
		}

		public string InnerText()
		{
			var nodes = document.DocumentNode.SelectNodes("//text");
			var sb = new StringBuilder();
			if(nodes != null)
				foreach (var item in nodes)
					sb.AppendLine(item.InnerText);
			return sb.ToString();
		}*/




	}
}
